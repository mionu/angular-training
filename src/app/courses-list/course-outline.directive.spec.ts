import { CourseOutlineDirective } from './course-outline.directive';

describe('CourseOutlineDirective', () => {
  it('should create an instance', () => {
    const directive = new CourseOutlineDirective();
    expect(directive).toBeTruthy();
  });
});
